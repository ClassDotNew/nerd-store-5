<% if @product.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>
 
    <ul>
    <% @product.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>


<%= form_tag url, method: form_method do %>
<% array_of_hashes = [
  {"key1" => "value1"}, %>
  <% {"key2" => "value2"}, %>
  <% {"final_key" => "final_value"} %>
  <% ] %>
<%= array_of_hashes %>



  <div>
  <% array_of_hashes.each do |hash| %>
    <% hash.each do |key,value| %>
      <input class="test" type="hidden" name="the_values[<%= key %>]" value="<%= value %>">
    <% end %>
  <% end %>
  </div>


  <div>
    <%= label_tag :name %>
    <%= text_field_tag :name, @product.name %>
  </div>

  <div>
    <%= label_tag :price %>
    <%= text_field_tag :price, @product.price %>
  </div>

  <div>
    <%= label_tag :image %>
    <%= text_field_tag :image, @product.image %>
  </div>

  <div>
    <%= label_tag :description %>
    <%= text_field_tag :description, @product.description %>
  </div>

  <div><%= collection_select :supplier, :supplier_id, Supplier.all, :id, :name, selected: @product.supplier_id %>
  </div>

 <%= submit_tag message %>

<% end %>
